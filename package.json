{
  "name": "test-guardian",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @test-guardian/cli dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
